<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <title>Register</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100 flex items-center justify-center min-h-screen">

  <div class="w-full max-w-2xl p-6">

    <!-- Main Container -->
    <div class="bg-white shadow-lg rounded-2xl w-full p-8">

      <h1 class="text-2xl font-bold text-center text-gray-800 mb-6">Créer un compte</h1>

      <form action="/auth/register" method="POST" enctype="multipart/form-data"
        class="grid grid-cols-1 md:grid-cols-2 gap-4">

        <!-- Name -->
        <div>
          <input type="text" name="displayName" placeholder="Nom complet" required
            class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 text-sm" />
        </div>

        <!-- Email -->
        <div>
          <input type="email" name="email" placeholder="Email" required
            class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 text-sm" />
        </div>

        <!-- Password -->
        <div>
          <input type="password" name="password" placeholder="Mot de passe" required
            class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 text-sm" />
        </div>

        <!-- Currency -->
        <div>
          <select name="currency" required
            class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 text-sm cursor-pointer">
            <option value="MAD">🇲🇦 MAD</option>
            <option value="USD">🇺🇸 USD</option>
            <option value="EUR">🇪🇺 EUR</option>
            <option value="GBP">🇬🇧 GBP</option>
            <option value="JPY">🇯🇵 JPY</option>
          </select>
        </div>

        <!-- Picture -->
        <div class="md:col-span-2">
          <input type="file" name="picture" accept="image/*"
            class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 text-sm" />
        </div>

        <!-- Submit Button -->
        <div class="md:col-span-2">
          <button type="submit"
            class="w-full bg-blue-600 text-white font-semibold py-2 rounded-lg hover:bg-blue-700 transition duration-200">
            Créer mon compte
          </button>
        </div>

      </form>

      <!-- Login Link -->
      <p class="text-center text-gray-600 text-sm mt-6">
        Vous avez déjà un compte ?
        <a href="/auth/login" class="text-blue-600 hover:underline">Se connecter</a>
      </p>

    </div>
  </div>

  <!-- Flash Messages Script -->
  <script>
    window.addEventListener('DOMContentLoaded', () => {
      const flashMessages = [];

    <% if (success_msg && success_msg.length > 0) { %>
        flashMessages.push({ type: 'success', text: "<%= success_msg %>" });
    <% } %>

    <% if (error_msg && error_msg.length > 0) { %>
      <% error_msg.forEach(err => { %>
          flashMessages.push({ type: 'error', text: "<%= typeof err === 'string' ? err : err.message %>" });
      <% }) %>
    <% } %>

    if (flashMessages.length > 0) {
        showFlashMessages(flashMessages); // call function from flashMessages.js
      }
    });
  </script>

  <!-- import the js file -->
  <script src="/js/flashMessages.js"></script>



</body>

</html>