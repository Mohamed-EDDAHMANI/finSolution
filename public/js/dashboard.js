document.addEventListener("DOMContentLoaded", () => {
  if (window.initCharts) initCharts();
  if (window.initTransactions) initTransactions();
  if (window.initCategories) initCategories();
  if (window.initBudgets) initBudgets();
  if (window.initCharts) initCharts();
  if (window.initLogout) initLogout();

});
